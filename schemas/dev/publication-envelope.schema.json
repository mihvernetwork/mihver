{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mihver.network/schemas/dev/publication-envelope.schema.json",
  "title": "MIHVER Publication Envelope",
  "description": "Machine-readable transport contract for MIHVER's privilege-separated publication architecture (V3.1-A). Produced only by Claude, per docs/development/CODEX_ROLES.md's 'Publication Envelope' section (that document's prose is the semantic owner; this schema is its machine-readable shape). Consumed by the deterministic Local Publication Builder (scripts/dev/publication-builder.mjs) to produce a local commit only -- never a push, never an authenticated GitHub call. The same Envelope shape is also the transport contract for the future privileged Publication Broker (V3.1-B, not implemented by this schema or any code in this task).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol_version", "repository", "branch", "base_branch", "base_commit",
    "expected_pre_publish_head", "allowed_files", "publication_fingerprint",
    "commit_message", "pr_expected"
  ],
  "properties": {
    "protocol_version": { "const": "1.0.0" },
    "repository": {
      "type": "object",
      "additionalProperties": false,
      "required": ["remote_name", "owner", "name"],
      "properties": {
        "remote_name": { "type": "string", "minLength": 1, "description": "The git remote this identity was read from, normally 'origin'." },
        "owner": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 }
      }
    },
    "branch": { "type": "string", "minLength": 1, "not": { "enum": ["main", "master"] } },
    "base_branch": { "type": "string", "minLength": 1 },
    "base_commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "expected_pre_publish_head": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "allowed_files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "action"],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "not": { "pattern": "(^/|\\\\|\\.\\.|\\x00|\\n)" },
            "description": "Repo-relative path, forward slashes, no leading slash, no '..' segment, no NUL/newline."
          },
          "action": { "enum": ["present", "deletion"], "description": "'present' = shape (A) present regular file; 'deletion' = shape (B) authorized deletion. A rename is two entries: old path 'deletion', new path 'present'." }
        }
      }
    },
    "publication_fingerprint": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hex digest computed per AGENT_POLICY.md's 'Publication Envelope' canonical recipe over exactly allowed_files."
    },
    "commit_message": { "type": "string", "minLength": 1 },
    "pr_expected": { "type": "boolean" },
    "pr_title": { "type": "string", "minLength": 1, "description": "Required (non-schema-enforced) when pr_expected is true." },
    "pr_body": { "type": "string", "description": "Required (non-schema-enforced) when pr_expected is true. Verbatim text, or a deterministic template with mechanically-filled fields -- never content Git Operator/the broker composes itself." }
  }
}
